<Window x:Class="WallpaperManager.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WallpaperManager.ViewModels"
        xmlns:conv="clr-namespace:WallpaperManager.Converters"
        xmlns:models="clr-namespace:WallpaperManager.Models"
        xmlns:sys="clr-namespace:System;assembly=System.Runtime"
        xmlns:controls="clr-namespace:WallpaperManager.Controls"
        Title="Wallpaper Manager" 
        Height="700" Width="1050"
        MinHeight="550" MinWidth="850"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        ResizeMode="CanResize"
        Icon="/Resources/app.ico"
        Loaded="Window_Loaded"
        AllowDrop="True"
        Drop="Window_Drop"
        DragOver="Window_DragOver">
    
    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:PathToThumbnailConverter x:Key="PathToThumb"/>
        <conv:AsyncThumbnailConverter x:Key="AsyncThumb"/>
        <conv:UnsplashUrlToImageConverter x:Key="UrlToImage"/>
        <conv:WallpaperTypeToIconConverter x:Key="TypeToIcon"/>
        <conv:WallpaperTypeToVisibilityConverter x:Key="TypeToVis"/>
        <conv:NullToVisibilityConverter x:Key="NullToVis"/>
        <conv:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <conv:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <conv:MultiSelectToVisibilityConverter x:Key="MultiSelectToVis"/>
        <!-- Converters pour section Dynamiques -->
        <conv:IsActiveDynamicConverter x:Key="IsActiveDynamicConverter"/>
        <conv:TimelinePositionConverter x:Key="TimelinePositionConverter"/>
        <conv:FloorConverter x:Key="FloorConverter"/>
        <conv:FractionToMinutesConverter x:Key="FractionToMinutesConverter"/>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" Padding="4,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Margin="20,20,16,20">
                    <TextBlock Text="🎨 Wallpaper" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="Manager" FontSize="22" FontWeight="Light" Foreground="{StaticResource PrimaryBrush}" Margin="0,-4,0,0"/>
                </StackPanel>
                
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="12,0,8,0">
                    <RadioButton x:Name="tabLibrary" Content="📚  Bibliothèque" IsChecked="True"
                                 Style="{StaticResource NavTabStyle}" Margin="0,4"/>
                    <RadioButton x:Name="tabCollections" Content="📂  Collections"
                                 Style="{StaticResource NavTabStyle}" Margin="0,4"/>
                    <RadioButton x:Name="tabDynamic" Content="🌅  Dynamiques"
                                 Style="{StaticResource NavTabStyle}" Margin="0,4"/>
                    
                    <Separator Margin="8,12" Background="{StaticResource BorderBrush}"/>
                    <TextBlock Text="SOURCES EN LIGNE" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" Margin="12,0,0,4" FontWeight="SemiBold"/>
                    
                    <RadioButton x:Name="tabUnsplash" Style="{StaticResource NavTabStyle}" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🌐  Unsplash"/>
                            <Ellipse Width="8" Height="8" Margin="8,0,0,0" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#EF4444"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsUnsplashConfigured}" Value="True">
                                                <Setter Property="Fill" Value="#22C55E"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="tabPexels" Style="{StaticResource NavTabStyle}" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📷  Pexels"/>
                            <Ellipse Width="8" Height="8" Margin="8,0,0,0" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#EF4444"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsPexelsConfigured}" Value="True">
                                                <Setter Property="Fill" Value="#22C55E"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="tabPixabay" Style="{StaticResource NavTabStyle}" Margin="0,4">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🖼️  Pixabay"/>
                            <Ellipse Width="8" Height="8" Margin="8,0,0,0" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#EF4444"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsPixabayConfigured}" Value="True">
                                                <Setter Property="Fill" Value="#22C55E"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </StackPanel>
                    </RadioButton>
                    
                    <Separator Margin="8,12" Background="{StaticResource BorderBrush}"/>
                    
                    <RadioButton x:Name="tabSettings" Content="⚙️  Paramètres"
                                 Style="{StaticResource NavTabStyle}" Margin="0,4"/>
                    
                    <Separator Margin="8,16" Background="{StaticResource BorderBrush}"/>
                    
                    <!-- Section visible uniquement sur l'onglet Bibliothèque -->
                    <StackPanel Visibility="{Binding IsChecked, ElementName=tabLibrary, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="Rotation automatique" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="12,8,12,4"/>
                        
                        <ToggleButton IsChecked="{Binding IsRotationEnabled, Mode=TwoWay}" Margin="12,4,8,4" Padding="12,8" Cursor="Hand">
                            <ToggleButton.Style>
                                <Style TargetType="ToggleButton">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border x:Name="border" Background="{StaticResource CardBrush}" CornerRadius="8" Padding="12,8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock x:Name="label" Text="Désactivé" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                        <Border Grid.Column="1" Width="44" Height="24" CornerRadius="12" Background="{StaticResource BorderBrush}" x:Name="toggle">
                                                            <Ellipse Width="20" Height="20" Fill="White" HorizontalAlignment="Left" Margin="2,0" x:Name="dot"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="toggle" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter TargetName="dot" Property="HorizontalAlignment" Value="Right"/>
                                                        <Setter TargetName="label" Property="Text" Value="Activé"/>
                                                        <Setter TargetName="label" Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ToggleButton.Style>
                        </ToggleButton>
                        
                        <TextBlock Text="Changer de fond" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="12,12,12,4"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="12,4" HorizontalAlignment="Left">
                            <Button Content="◀" Command="{Binding PreviousWallpaperCommand}" Width="36" Height="36" Style="{StaticResource PrimaryButton}" Padding="0" Margin="0,0,4,0" ToolTip="Précédent"/>
                            <Button Content="▶" Command="{Binding NextWallpaperCommand}" Width="36" Height="36" Style="{StaticResource PrimaryButton}" Padding="0" ToolTip="Suivant"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
                </ScrollViewer>
                
                <Border Grid.Row="2" Background="{StaticResource CardBrush}" Margin="12,8" CornerRadius="8" Padding="12,8">
                    <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                </Border>
            </Grid>
        </Border>

        <!-- Contenu principal -->
        <Grid Grid.Column="1" Margin="20">
            
            <!-- BIBLIOTHÈQUE -->
            <Grid Visibility="{Binding IsChecked, ElementName=tabLibrary, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Ma bibliothèque" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="12,0,0,0">
                            <Run Text="{Binding FilteredCount, Mode=OneWay}"/>/<Run Text="{Binding TotalCount, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>
                    <Button Grid.Column="1" Content="+ Ajouter" Style="{StaticResource PrimaryButton}" Command="{Binding AddWallpapersCommand}"/>
                </Grid>
                
                <!-- Filtres -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="6" Margin="0,0,0,12">
                    <Grid Height="26">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid>
                            <TextBox x:Name="SearchBox" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Background" Value="#1A1A2E"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderBrush" Value="#374151"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="FontFamily" Value="Segoe UI"/>
                                        <Setter Property="FontSize" Value="13"/>
                                        <Setter Property="Padding" Value="8,4"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="0,0,8,0"/>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                            <TextBlock Text="🔍 Rechercher..." 
                                       Foreground="#9CA3AF" 
                                       FontSize="13" 
                                       VerticalAlignment="Center" 
                                       Margin="10,0,0,0" 
                                       IsHitTestVisible="False"
                                       Visibility="{Binding Text, ElementName=SearchBox, Converter={StaticResource NullToVis}, ConverterParameter=Invert}"/>
                        </Grid>
                        <CheckBox Grid.Column="1" Content="★ Favoris" IsChecked="{Binding FilterFavoritesOnly}" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="13"/>
                        <ComboBox Grid.Column="2" ItemsSource="{Binding SortOptions}" SelectedItem="{Binding SortBy}" Width="70" Height="26" FontSize="13" Margin="0,0,8,0" Padding="6,2" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="3" Content="✕" Width="26" Height="26" Padding="0" FontSize="13" Style="{StaticResource PrimaryButton}" Command="{Binding ClearFiltersCommand}" ToolTip="Réinitialiser"/>
                    </Grid>
                </Border>
                
                <!-- Grille wallpapers avec virtualisation -->
                <ListBox Grid.Row="2" x:Name="WallpaperListBox" ItemsSource="{Binding Wallpapers}" SelectedItem="{Binding SelectedWallpaper}" SelectionMode="Extended" SelectionChanged="WallpaperListBox_SelectionChanged" Background="Transparent" BorderThickness="0"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         VirtualizingPanel.ScrollUnit="Pixel"
                         ScrollViewer.IsDeferredScrollingEnabled="True"
                         ScrollViewer.CanContentScroll="True">
                    <ListBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Ajouter à une collection" ItemsSource="{Binding DataContext.Collections, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                <MenuItem.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock>
                                            <Run Text="{Binding Icon}"/> <Run Text="{Binding Name}"/>
                                        </TextBlock>
                                    </DataTemplate>
                                </MenuItem.ItemTemplate>
                                <MenuItem.ItemContainerStyle>
                                    <Style TargetType="MenuItem">
                                        <Setter Property="Command" Value="{Binding DataContext.AddToCollectionCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <Setter Property="CommandParameter" Value="{Binding}"/>
                                    </Style>
                                </MenuItem.ItemContainerStyle>
                            </MenuItem>
                            <Separator/>
                            <MenuItem Header="👁️ Prévisualiser" Command="{Binding DataContext.PreviewWallpaperCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <MenuItem Header="★ Favoris" Command="{Binding DataContext.ToggleFavoriteSelectedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <MenuItem Header="📂 Ouvrir dans l'explorateur" Command="{Binding DataContext.OpenInExplorerCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <Separator/>
                            <MenuItem Header="🗑️ Supprimer" Command="{Binding DataContext.DeleteSelectedCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        </ContextMenu>
                    </ListBox.ContextMenu>
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <controls:VirtualizingWrapPanel ItemWidth="188" ItemHeight="128" ItemMargin="4"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Width="180" Height="120" CornerRadius="8" Background="{StaticResource CardBrush}" Cursor="Hand" MouseLeftButtonDown="WallpaperItem_MouseLeftButtonDown">
                                <Grid>
                                    <Image Source="{Binding FilePath, Converter={StaticResource AsyncThumb}}" Stretch="UniformToFill"/>
                                    
                                    <!-- Icône play au centre pour les vidéos -->
                                    <Border Background="#60000000" CornerRadius="50" Width="40" Height="40"
                                            HorizontalAlignment="Center" VerticalAlignment="Center"
                                            Visibility="{Binding Type, Converter={StaticResource TypeToVis}, ConverterParameter=Video}">
                                        <TextBlock Text="▶" FontSize="18" Foreground="White" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,0,0,0"/>
                                    </Border>
                                    
                                    <Border Background="#80000000" VerticalAlignment="Bottom" CornerRadius="0,0,8,8" Padding="8,4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Type, Converter={StaticResource TypeToIcon}}" FontSize="12" Margin="0,0,4,0"/>
                                            <TextBlock Grid.Column="1" Text="{Binding DisplayName}" FontSize="11" Foreground="White" TextTrimming="CharacterEllipsis"/>
                                        </Grid>
                                    </Border>
                                    <Border Background="{StaticResource PrimaryBrush}" HorizontalAlignment="Right" VerticalAlignment="Top" CornerRadius="0,8,0,8" Padding="6,2" Visibility="{Binding IsFavorite, Converter={StaticResource BoolToVis}}">
                                        <TextBlock Text="★" Foreground="White" FontSize="10"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                
                <!-- Actions sélection simple -->
                <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,12,0,0" Visibility="{Binding SelectedWallpaper, Converter={StaticResource NullToVis}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Image Source="{Binding SelectedWallpaper.FilePath, Converter={StaticResource PathToThumb}}" Width="100" Height="60" Stretch="UniformToFill" Margin="0,0,16,0" Cursor="Hand" MouseLeftButtonDown="PreviewImage_Click"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding SelectedWallpaper.DisplayName}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock FontSize="12" Foreground="{StaticResource TextSecondaryBrush}">
                                <Run Text="{Binding SelectedWallpaper.Resolution}"/> • <Run Text="{Binding SelectedWallpaper.FileSizeFormatted}"/>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button Content="👁️" Command="{Binding PreviewWallpaperCommand}" Width="36" Height="36" Margin="0,0,8,0" Style="{StaticResource PrimaryButton}" Padding="0" ToolTip="Prévisualiser"/>
                            <Button Content="★" Command="{Binding ToggleFavoriteCommand}" Width="36" Height="36" Margin="0,0,8,0" Style="{StaticResource PrimaryButton}" Padding="0" ToolTip="Favoris"/>
                            
                            <!-- Bouton Ajouter à la collection avec menu déroulant -->
                            <Button x:Name="BtnAddToCollection" Content="📌" Width="36" Height="36" Margin="0,0,8,0" Style="{StaticResource PrimaryButton}" Padding="0" ToolTip="Ajouter à une collection" Click="BtnAddToCollection_Click">
                                <Button.ContextMenu>
                                    <ContextMenu x:Name="CollectionMenu" ItemsSource="{Binding Collections}" Placement="Top">
                                        <ContextMenu.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock>
                                                    <Run Text="{Binding Icon}"/> <Run Text="{Binding Name}"/>
                                                </TextBlock>
                                            </DataTemplate>
                                        </ContextMenu.ItemTemplate>
                                        <ContextMenu.ItemContainerStyle>
                                            <Style TargetType="MenuItem">
                                                <Setter Property="Command" Value="{Binding DataContext.AddToCollectionCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <Setter Property="CommandParameter" Value="{Binding}"/>
                                            </Style>
                                        </ContextMenu.ItemContainerStyle>
                                    </ContextMenu>
                                </Button.ContextMenu>
                            </Button>
                            
                            <Button Content="📂" Command="{Binding OpenInExplorerCommand}" Width="36" Height="36" Margin="0,0,8,0" Style="{StaticResource PrimaryButton}" Padding="0" ToolTip="Ouvrir dossier"/>
                            <Button Content="Appliquer" Command="{Binding ApplyWallpaperCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                            <Button Content="🗑️" Command="{Binding RemoveWallpaperCommand}" Width="36" Height="36" Background="#EF4444" Style="{StaticResource PrimaryButton}" Padding="0" ToolTip="Supprimer"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Actions multi-sélection -->
                <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,12,0,0" Background="#1E3A5F" Visibility="{Binding SelectedCount, Converter={StaticResource MultiSelectToVis}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock VerticalAlignment="Center" Foreground="White" FontSize="14">
                            <Run Text="{Binding SelectedCount, Mode=OneWay}"/> élément(s) sélectionné(s)
                        </TextBlock>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="★ Favoris" Command="{Binding ToggleFavoriteSelectedCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                            
                            <!-- Bouton Ajouter à la collection (multi-sélection) -->
                            <Button x:Name="BtnAddToCollectionMulti" Content="📌 Collection" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" Click="BtnAddToCollection_Click">
                                <Button.ContextMenu>
                                    <ContextMenu ItemsSource="{Binding Collections}" Placement="Top">
                                        <ContextMenu.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock>
                                                    <Run Text="{Binding Icon}"/> <Run Text="{Binding Name}"/>
                                                </TextBlock>
                                            </DataTemplate>
                                        </ContextMenu.ItemTemplate>
                                        <ContextMenu.ItemContainerStyle>
                                            <Style TargetType="MenuItem">
                                                <Setter Property="Command" Value="{Binding DataContext.AddToCollectionCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <Setter Property="CommandParameter" Value="{Binding}"/>
                                            </Style>
                                        </ContextMenu.ItemContainerStyle>
                                    </ContextMenu>
                                </Button.ContextMenu>
                            </Button>
                            
                            <Button Content="🗑️ Supprimer" Command="{Binding DeleteSelectedCommand}" Style="{StaticResource PrimaryButton}" Background="#EF4444"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Drop overlay -->
                <Border Grid.Row="2" x:Name="DropOverlay" Background="#CC1E293B" CornerRadius="12" Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="📥" FontSize="64" HorizontalAlignment="Center"/>
                        <TextBlock Text="Déposez vos fichiers ici" FontSize="20" Foreground="White" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- COLLECTIONS -->
            <Grid Visibility="{Binding IsChecked, ElementName=tabCollections, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Grid Grid.ColumnSpan="2" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Collections" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Organisez vos fonds d'écran par thème" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="+ Nouvelle collection" Style="{StaticResource PrimaryButton}" Command="{Binding CreateCollectionCommand}"/>
                </Grid>
                
                <!-- Liste des collections -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,16,0">
                    <ListBox ItemsSource="{Binding Collections}" SelectedItem="{Binding SelectedCollection}" Background="Transparent" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12" Margin="2" CornerRadius="8" Background="{StaticResource SurfaceBrush}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Icon}" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                            <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                                                <Run Text="{Binding Count, Mode=OneWay}"/> fond(s) d'écran
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
                
                <!-- Détail de la collection -->
                <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardStyle}" Visibility="{Binding SelectedCollection, Converter={StaticResource NullToVis}}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- En-tête de la collection -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Icône fixe pour Favoris -->
                            <TextBlock Text="{Binding SelectedCollection.Icon}" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"
                                       Visibility="{Binding IsSelectedCollectionFavorites, Converter={StaticResource BoolToVis}}"/>
                            
                            <!-- ComboBox pour les autres collections -->
                            <ComboBox Width="50" Height="36" SelectedItem="{Binding SelectedCollection.Icon, Mode=TwoWay}" FontSize="20" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Margin="0,0,8,0"
                                      Visibility="{Binding CanEditSelectedCollection, Converter={StaticResource BoolToVis}}">
                                <sys:String>📁</sys:String>
                                <sys:String>🎨</sys:String>
                                <sys:String>🌙</sys:String>
                                <sys:String>☀️</sys:String>
                                <sys:String>🎮</sys:String>
                                <sys:String>🏞️</sys:String>
                                <sys:String>🌊</sys:String>
                                <sys:String>🔥</sys:String>
                                <sys:String>❄️</sys:String>
                                <sys:String>🌸</sys:String>
                                <sys:String>🍂</sys:String>
                                <sys:String>⭐</sys:String>
                                <sys:String>🌄</sys:String>
                                <sys:String>🌃</sys:String>
                                <sys:String>🖥️</sys:String>
                                <sys:String>🎬</sys:String>
                            </ComboBox>
                            
                            <!-- Nom fixe pour Favoris -->
                            <TextBlock Grid.Column="1" Text="{Binding SelectedCollection.Name}" FontSize="18" FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"
                                       Visibility="{Binding IsSelectedCollectionFavorites, Converter={StaticResource BoolToVis}}"/>
                            
                            <!-- TextBox pour les autres collections -->
                            <TextBox Grid.Column="1" Text="{Binding SelectedCollection.Name, UpdateSourceTrigger=PropertyChanged}" 
                                     Style="{StaticResource ModernTextBox}" FontSize="18" FontWeight="SemiBold"
                                     Visibility="{Binding CanEditSelectedCollection, Converter={StaticResource BoolToVis}}"/>
                            
                            <!-- Bouton supprimer (caché pour Favoris) -->
                            <Button Grid.Column="2" Content="🗑️" Command="{Binding DeleteCollectionCommand}" 
                                    Background="#EF4444" Style="{StaticResource PrimaryButton}" 
                                    Width="36" Height="36" Padding="0" Margin="8,0,0,0" 
                                    ToolTip="Supprimer la collection"
                                    Visibility="{Binding CanEditSelectedCollection, Converter={StaticResource BoolToVis}}"/>
                        </Grid>
                        
                        <!-- Info -->
                        <TextBlock Grid.Row="1" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,12">
                            <Run Text="{Binding CollectionWallpapers.Count, Mode=OneWay}"/> fond(s) d'écran dans cette collection
                        </TextBlock>
                        
                        <!-- Grille des wallpapers de la collection -->
                        <ListBox Grid.Row="2" ItemsSource="{Binding CollectionWallpapers}" SelectedItem="{Binding SelectedCollectionWallpaper}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="160" Height="100" Margin="4" CornerRadius="8" Background="{StaticResource CardBrush}" Cursor="Hand">
                                        <Grid>
                                            <Image Source="{Binding FilePath, Converter={StaticResource PathToThumb}}" Stretch="UniformToFill"/>
                                            
                                            <!-- Icône play au centre pour les vidéos -->
                                            <Border Background="#60000000" CornerRadius="50" Width="36" Height="36"
                                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                                    Visibility="{Binding Type, Converter={StaticResource TypeToVis}, ConverterParameter=Video}">
                                                <TextBlock Text="▶" FontSize="16" Foreground="White" 
                                                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,0,0,0"/>
                                            </Border>
                                            
                                            <Border Background="#80000000" VerticalAlignment="Bottom" CornerRadius="0,0,8,8" Padding="8,4">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Type, Converter={StaticResource TypeToIcon}}" FontSize="10" Margin="0,0,4,0"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding DisplayName}" FontSize="10" Foreground="White" TextTrimming="CharacterEllipsis"/>
                                                    <Button Grid.Column="2" Content="✖" FontSize="10" Width="18" Height="18" Padding="0" 
                                                            Command="{Binding DataContext.RemoveFromCollectionCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                            CommandParameter="{Binding}" 
                                                            Background="#EF4444" Style="{StaticResource PrimaryButton}" ToolTip="Retirer"/>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        
                        <!-- Bouton Rotation collection (toujours visible quand collection sélectionnée) -->
                        <Border Grid.Row="3" Margin="0,12,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Actions quand un wallpaper est sélectionné -->
                                <StackPanel Orientation="Horizontal" Visibility="{Binding SelectedCollectionWallpaper, Converter={StaticResource NullToVis}}">
                                    <Button Content="👁️ Prévisualiser" Command="{Binding PreviewCollectionWallpaperCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                                    <Button Content="Appliquer" Command="{Binding ApplyCollectionWallpaperCommand}" Style="{StaticResource PrimaryButton}"/>
                                </StackPanel>
                                
                                <!-- Bouton rotation toujours visible à droite -->
                                <Button Grid.Column="1" Content="▶ Rotation collection" Command="{Binding StartCollectionRotationCommand}" Style="{StaticResource PrimaryButton}"/>
                            </Grid>
                        </Border>
                        
                        <!-- Message si vide -->
                        <TextBlock Grid.Row="2" Text="Glissez des fonds d'écran depuis la bibliothèque ou utilisez le menu contextuel" 
                                   Foreground="{StaticResource TextSecondaryBrush}" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="{Binding CollectionWallpapers.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}"/>
                    </Grid>
                </Border>
                
                <!-- Message si aucune collection -->
                <Border Grid.Row="1" Grid.ColumnSpan="2" Visibility="{Binding SelectedCollection, Converter={StaticResource NullToVis}, ConverterParameter=Invert}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="📂" FontSize="64" HorizontalAlignment="Center" Opacity="0.5"/>
                        <TextBlock Text="Sélectionnez ou créez une collection" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- DYNAMIQUES -->
            <Grid Visibility="{Binding IsChecked, ElementName=tabDynamic, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- En-tête -->
                <Grid Grid.ColumnSpan="2" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="🌅 Wallpapers Dynamiques" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Changent automatiquement selon l'heure ou le soleil" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="📥" Style="{StaticResource PrimaryButton}" Command="{Binding ImportDynamicWallpaperCommand}" Width="36" Margin="0,0,8,0" ToolTip="Importer un pack"/>
                        <Button Content="+ 2" Style="{StaticResource PrimaryButton}" Command="{Binding CreateDynamic2PeriodsCommand}" Width="40" Margin="0,0,4,0" ToolTip="Jour/Nuit"/>
                        <Button Content="+ 4" Style="{StaticResource PrimaryButton}" Command="{Binding CreateDynamic4PeriodsCommand}" Width="40" Margin="0,0,4,0" ToolTip="4 périodes"/>
                        <Button Content="+ 6" Style="{StaticResource PrimaryButton}" Command="{Binding CreateDynamic6PeriodsCommand}" Width="40" Margin="0,0,4,0" ToolTip="6 périodes"/>
                        <Button Content="☀️ Soleil" Style="{StaticResource PrimaryButton}" Command="{Binding CreateSunBasedDynamicCommand}" ToolTip="Basé sur lever/coucher du soleil"/>
                    </StackPanel>
                </Grid>
                
                <!-- Liste des wallpapers dynamiques -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,16,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ListBox ItemsSource="{Binding DynamicWallpapers}" SelectedItem="{Binding SelectedDynamicWallpaper}" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="12" Margin="2" CornerRadius="8" Cursor="Hand">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Id, Converter={StaticResource IsActiveDynamicConverter}}" Value="True">
                                                        <Setter Property="Background" Value="#1E3A5F"/>
                                                        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                        <Setter Property="BorderThickness" Value="2"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="50"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Preview thumbnail -->
                                            <Border Width="44" Height="32" CornerRadius="4" Background="{StaticResource CardBrush}">
                                                <Image Source="{Binding PreviewPath, Converter={StaticResource PathToThumb}}" Stretch="UniformToFill"/>
                                            </Border>
                                            
                                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    <!-- Badge mode -->
                                                    <Border Background="{StaticResource PrimaryBrush}" CornerRadius="4" Padding="4,1" Margin="6,0,0,0" VerticalAlignment="Center"
                                                            Visibility="{Binding IsSunBased, Converter={StaticResource BoolToVis}}">
                                                        <TextBlock Text="☀️" FontSize="9"/>
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                                                    <Run Text="{Binding ConfigurationStatus, Mode=OneWay}"/>
                                                </TextBlock>
                                            </StackPanel>
                                            
                                            <!-- Indicateur actif -->
                                            <Border Grid.Column="2" Width="10" Height="10" CornerRadius="5" VerticalAlignment="Center"
                                                    Visibility="{Binding Id, Converter={StaticResource IsActiveDynamicConverter}}">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#22C55E"/>
                                                    </Style>
                                                </Border.Style>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        
                        <!-- Message si liste vide -->
                        <TextBlock Grid.Row="0" Text="Aucun wallpaper dynamique" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="{Binding DynamicWallpapers.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}"/>
                    </Grid>
                </Border>
                
                <!-- Panneau de configuration -->
                <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardStyle}" Visibility="{Binding SelectedDynamicWallpaper, Converter={StaticResource NullToVis}}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- En-tête: Nom et options -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding SelectedDynamicWallpaper.Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" FontSize="18" FontWeight="SemiBold"/>
                            <Button Grid.Column="1" Content="📤" Command="{Binding ExportDynamicWallpaperCommand}" Style="{StaticResource PrimaryButton}" Width="36" Margin="8,0,0,0" ToolTip="Exporter"/>
                            <Button Grid.Column="2" Content="📋" Command="{Binding DuplicateDynamicWallpaperCommand}" Style="{StaticResource PrimaryButton}" Width="36" Margin="8,0,0,0" ToolTip="Dupliquer"/>
                        </Grid>
                        
                        <!-- Options: Mode, Transition -->
                        <Grid Grid.Row="1" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Text="Mode:" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox Grid.Column="1" ItemsSource="{Binding DynamicModes}" SelectedItem="{Binding SelectedDynamicWallpaper.Mode}" Height="28"/>
                            
                            <TextBlock Grid.Column="2" Text="Transition:" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="16,0,8,0"/>
                            <ComboBox Grid.Column="3" ItemsSource="{Binding DynamicTransitionTypes}" SelectedItem="{Binding SelectedDynamicWallpaper.TransitionType}" Height="28"/>
                            
                            <TextBlock Grid.Column="4" Text="Durée:" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="16,0,8,0"/>
                            <TextBox Grid.Column="5" Text="{Binding SelectedDynamicWallpaper.TransitionDuration}" Style="{StaticResource ModernTextBox}" Height="28"/>
                            
                            <!-- Bouton recalculer pour mode Soleil -->
                            <Button Grid.Column="6" Content="🔄 Recalculer" Command="{Binding RecalculateSunTimesCommand}" Style="{StaticResource PrimaryButton}" 
                                    HorizontalAlignment="Right" Visibility="{Binding SelectedDynamicWallpaper.IsSunBased, Converter={StaticResource BoolToVis}}"/>
                        </Grid>
                        
                        <!-- Timeline visuelle 24h -->
                        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="12,8" Margin="0,0,0,12">
                            <Grid Height="40">
                                <!-- Fond gradient jour/nuit -->
                                <Border CornerRadius="4" ClipToBounds="True">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#1a1a2e" Offset="0"/>
                                            <GradientStop Color="#1a1a2e" Offset="0.2"/>
                                            <GradientStop Color="#4a69bd" Offset="0.25"/>
                                            <GradientStop Color="#f6b93b" Offset="0.35"/>
                                            <GradientStop Color="#87ceeb" Offset="0.5"/>
                                            <GradientStop Color="#f6b93b" Offset="0.75"/>
                                            <GradientStop Color="#4a69bd" Offset="0.85"/>
                                            <GradientStop Color="#1a1a2e" Offset="0.9"/>
                                            <GradientStop Color="#1a1a2e" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>
                                
                                <!-- Marqueurs d'heures -->
                                <Canvas>
                                    <TextBlock Text="0h" Foreground="White" FontSize="9" Canvas.Left="0" Canvas.Top="28" Opacity="0.7"/>
                                    <TextBlock Text="6h" Foreground="White" FontSize="9" Canvas.Left="25%" Canvas.Top="28" Opacity="0.7"/>
                                    <TextBlock Text="12h" Foreground="White" FontSize="9" Canvas.Left="50%" Canvas.Top="28" Opacity="0.7"/>
                                    <TextBlock Text="18h" Foreground="White" FontSize="9" Canvas.Left="75%" Canvas.Top="28" Opacity="0.7"/>
                                    <TextBlock Text="24h" Foreground="White" FontSize="9" Canvas.Right="0" Canvas.Top="28" Opacity="0.7"/>
                                </Canvas>
                                
                                <!-- Indicateurs de variantes (cercles sur la timeline) -->
                                <ItemsControl ItemsSource="{Binding SelectedDynamicWallpaper.Variants}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <Canvas/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemContainerStyle>
                                        <Style TargetType="ContentPresenter">
                                            <Setter Property="Canvas.Left">
                                                <Setter.Value>
                                                    <MultiBinding Converter="{StaticResource TimelinePositionConverter}">
                                                        <Binding Path="TimelinePosition"/>
                                                        <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=ItemsControl}"/>
                                                    </MultiBinding>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="Canvas.Top" Value="5"/>
                                        </Style>
                                    </ItemsControl.ItemContainerStyle>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Ellipse Width="16" Height="16" Stroke="White" StrokeThickness="2" ToolTip="{Binding Label}" Cursor="Hand">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Fill" Value="#EF4444"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Exists}" Value="True">
                                                                <Setter Property="Fill" Value="#22C55E"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                        
                        <!-- Liste des variantes avec édition -->
                        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <!-- Bouton ajouter variante -->
                                <Button Content="+ Ajouter une période" Command="{Binding AddVariantCommand}" Style="{StaticResource PrimaryButton}" 
                                        HorizontalAlignment="Left" Margin="0,0,0,8"/>
                                
                                <ItemsControl ItemsSource="{Binding SelectedDynamicWallpaper.Variants}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <!-- Heure (ComboBox) -->
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <ComboBox ItemsSource="{Binding AvailableHours}" SelectedItem="{Binding StartHour}" Width="50" Height="28"/>
                                                        <TextBlock Text=":" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" Margin="2,0"/>
                                                        <ComboBox ItemsSource="{Binding AvailableMinutes}" SelectedItem="{Binding StartMinute}" Width="50" Height="28"/>
                                                    </StackPanel>
                                                    
                                                    <!-- Label éditable -->
                                                    <TextBox Grid.Column="1" Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" 
                                                             Width="90" Height="28" Margin="12,0,0,0" FontSize="12"/>
                                                    
                                                    <!-- Aperçu image -->
                                                    <Border Grid.Column="2" Height="50" CornerRadius="4" Margin="12,0" Background="{StaticResource CardBrush}" Cursor="Hand"
                                                            ToolTip="Cliquer pour prévisualiser">
                                                        <Border.InputBindings>
                                                            <MouseBinding MouseAction="LeftClick" 
                                                                          Command="{Binding DataContext.PreviewVariantCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                                          CommandParameter="{Binding}"/>
                                                        </Border.InputBindings>
                                                        <Grid>
                                                            <Image Source="{Binding FilePath, Converter={StaticResource PathToThumb}}" Stretch="UniformToFill"/>
                                                            <TextBlock Text="📷" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"
                                                                       Visibility="{Binding HasImage, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}"/>
                                                        </Grid>
                                                    </Border>
                                                    
                                                    <!-- Nom du fichier -->
                                                    <TextBlock Grid.Column="3" Text="{Binding FileName}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" 
                                                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,8,0"/>
                                                    
                                                    <!-- Actions -->
                                                    <StackPanel Grid.Column="4" Orientation="Horizontal">
                                                        <Button Content="📁" Command="{Binding DataContext.SetVariantImageCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                                CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}" Width="32" Height="28" Padding="0" ToolTip="Choisir fichier"/>
                                                        <Button Content="📚" Command="{Binding DataContext.SetVariantFromLibraryCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                                CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}" Width="32" Height="28" Padding="0" Margin="4,0,0,0" ToolTip="Depuis bibliothèque"/>
                                                        <Button Content="✕" Command="{Binding DataContext.ClearVariantImageCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                                CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}" Width="28" Height="28" Padding="0" Margin="4,0,0,0" ToolTip="Effacer image"/>
                                                        <Button Content="🗑️" Command="{Binding DataContext.RemoveVariantCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                                CommandParameter="{Binding}" Background="#EF4444" Style="{StaticResource PrimaryButton}" Width="28" Height="28" Padding="0" Margin="4,0,0,0" ToolTip="Supprimer période"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </ScrollViewer>
                        
                        <!-- Prévisualisation par heure -->
                        <Border Grid.Row="4" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="12" Margin="0,12,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="👁️ Prévisualiser à:" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                <Slider Grid.Column="1" Minimum="0" Maximum="24" Value="{Binding PreviewHour}" TickFrequency="1" IsSnapToTickEnabled="True" Margin="12,0"/>
                                <TextBlock Grid.Column="2" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}">
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0:00}:{1:00}">
                                            <Binding Path="PreviewHour" Converter="{StaticResource FloorConverter}"/>
                                            <Binding Path="PreviewHour" Converter="{StaticResource FractionToMinutesConverter}"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                                <Button Grid.Column="3" Content="👁️" Command="{Binding PreviewAtTimeCommand}" Style="{StaticResource PrimaryButton}" Width="36" Padding="0" ToolTip="Appliquer cette heure"/>
                            </Grid>
                        </Border>
                        
                        <!-- Actions principales -->
                        <Grid Grid.Row="5" Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Status -->
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="10" Height="10" CornerRadius="5" Margin="0,0,8,0"
                                        Visibility="{Binding IsSelectedDynamicActive, Converter={StaticResource BoolToVis}}">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#22C55E"/>
                                        </Style>
                                    </Border.Style>
                                </Border>
                                <TextBlock Text="Actif" Foreground="#22C55E" FontWeight="SemiBold" 
                                           Visibility="{Binding IsSelectedDynamicActive, Converter={StaticResource BoolToVis}}"/>
                            </StackPanel>
                            
                            <!-- Boutons -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="▶ Activer" Command="{Binding ActivateDynamicWallpaperCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                                <Button Content="⏹ Stop" Command="{Binding DeactivateDynamicWallpaperCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                                <Button Content="🗑️" Command="{Binding DeleteDynamicWallpaperCommand}" Background="#EF4444" Style="{StaticResource PrimaryButton}" Width="36" Padding="0"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
                
                <!-- Message si aucun wallpaper sélectionné -->
                <Border Grid.Row="1" Grid.Column="1" Visibility="{Binding SelectedDynamicWallpaper, Converter={StaticResource NullToVis}, ConverterParameter=Invert}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="🌅" FontSize="64" HorizontalAlignment="Center" Opacity="0.5"/>
                        <TextBlock Text="Sélectionnez ou créez un wallpaper dynamique" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Les wallpapers dynamiques changent automatiquement selon l'heure" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- UNSPLASH -->
            <Grid Visibility="{Binding IsChecked, ElementName=tabUnsplash, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Unsplash" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>
                <Grid Grid.Row="1" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding UnsplashSearchQuery, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Tag="Rechercher..."/>
                    <Button Grid.Column="1" Content="🔍 Rechercher" Command="{Binding SearchUnsplashCommand}" Style="{StaticResource PrimaryButton}" Margin="8,0"/>
                    <Button Grid.Column="2" Content="🎲 Aléatoire" Command="{Binding LoadRandomUnsplashCommand}" Style="{StaticResource PrimaryButton}"/>
                </Grid>
                <ListBox Grid.Row="2" ItemsSource="{Binding UnsplashPhotos}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Width="220" Height="140" Margin="4" CornerRadius="8" Background="{StaticResource CardBrush}" Cursor="Hand">
                                <Grid>
                                    <Image Source="{Binding Urls.Small, Converter={StaticResource UrlToImage}}" Stretch="UniformToFill"/>
                                    <Border Background="#80000000" VerticalAlignment="Bottom" CornerRadius="0,0,8,8" Padding="8,6">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="{Binding User.Name}" FontSize="11" Foreground="White" TextTrimming="CharacterEllipsis"/>
                                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,0">
                                                <Button Content="⬇️" FontSize="10" Padding="8,2" Margin="0,0,4,0" Command="{Binding DataContext.DownloadUnsplashPhotoCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}"/>
                                                <Button Content="Appliquer" FontSize="10" Padding="8,2" Command="{Binding DataContext.DownloadAndApplyUnsplashPhotoCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <!-- PEXELS -->
            <Grid Visibility="{Binding IsChecked, ElementName=tabPexels, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Pexels" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>
                <Grid Grid.Row="1" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="PexelsSearchBox" Style="{StaticResource ModernTextBox}" Tag="Rechercher sur Pexels..."/>
                    <Button Grid.Column="1" Content="🔍 Rechercher" Command="{Binding SearchPexelsCommand}" CommandParameter="{Binding Text, ElementName=PexelsSearchBox}" Style="{StaticResource PrimaryButton}" Margin="8,0"/>
                    <Button Grid.Column="2" Content="⭐ Populaires" Command="{Binding SearchPexelsCommand}" Style="{StaticResource PrimaryButton}"/>
                </Grid>
                <ListBox Grid.Row="2" ItemsSource="{Binding PexelsPhotos}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Width="220" Height="140" Margin="4" CornerRadius="8" Background="{StaticResource CardBrush}" Cursor="Hand">
                                <Grid>
                                    <Image Source="{Binding Src.Medium, Converter={StaticResource UrlToImage}}" Stretch="UniformToFill"/>
                                    <Border Background="#80000000" VerticalAlignment="Bottom" CornerRadius="0,0,8,8" Padding="8,6">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="{Binding Photographer}" FontSize="11" Foreground="White" TextTrimming="CharacterEllipsis"/>
                                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,0">
                                                <Button Content="⬇️" FontSize="10" Padding="8,2" Margin="0,0,4,0" Command="{Binding DataContext.DownloadPexelsPhotoCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}"/>
                                                <Button Content="Appliquer" FontSize="10" Padding="8,2" Command="{Binding DataContext.DownloadAndApplyPexelsPhotoCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <!-- PIXABAY -->
            <Grid Visibility="{Binding IsChecked, ElementName=tabPixabay, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Pixabay" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>
                <Grid Grid.Row="1" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="PixabaySearchBox" Style="{StaticResource ModernTextBox}" Tag="Rechercher sur Pixabay..."/>
                    <Button Grid.Column="1" Content="🔍 Rechercher" Command="{Binding SearchPixabayCommand}" CommandParameter="{Binding Text, ElementName=PixabaySearchBox}" Style="{StaticResource PrimaryButton}" Margin="8,0"/>
                    <Button Grid.Column="2" Content="⭐ Populaires" Command="{Binding SearchPixabayCommand}" Style="{StaticResource PrimaryButton}"/>
                </Grid>
                <ListBox Grid.Row="2" ItemsSource="{Binding PixabayPhotos}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Width="220" Height="140" Margin="4" CornerRadius="8" Background="{StaticResource CardBrush}" Cursor="Hand">
                                <Grid>
                                    <Image Source="{Binding PreviewUrl, Converter={StaticResource UrlToImage}}" Stretch="UniformToFill"/>
                                    <Border Background="#80000000" VerticalAlignment="Bottom" CornerRadius="0,0,8,8" Padding="8,6">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <TextBlock Text="{Binding User}" FontSize="11" Foreground="White" TextTrimming="CharacterEllipsis"/>
                                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,0">
                                                <Button Content="⬇️" FontSize="10" Padding="8,2" Margin="0,0,4,0" Command="{Binding DataContext.DownloadPixabayPhotoCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}"/>
                                                <Button Content="Appliquer" FontSize="10" Padding="8,2" Command="{Binding DataContext.DownloadAndApplyPixabayPhotoCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <!-- PARAMÈTRES -->
            <ScrollViewer Visibility="{Binding IsChecked, ElementName=tabSettings, Converter={StaticResource BoolToVis}}" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Paramètres" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>
                    
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Général" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                            <CheckBox Content="Démarrer avec Windows" IsChecked="{Binding StartWithWindows}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,8"/>
                            <CheckBox Content="Minimiser dans la barre des tâches" IsChecked="{Binding MinimizeToTray}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,8"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Rotation automatique" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                            <Grid Margin="0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Intervalle (minutes)" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding RotationInterval}" Width="80" Style="{StaticResource ModernTextBox}" TextAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Raccourcis clavier -->
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="⌨️ Raccourcis clavier" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                            <CheckBox Content="Activer les raccourcis globaux" IsChecked="{Binding HotkeysEnabled}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,8"/>
                            
                            <Grid Margin="0,8" IsEnabled="{Binding HotkeysEnabled}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Fond d'écran suivant" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding HotkeyNext, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" TextAlignment="Center"/>
                            </Grid>
                            
                            <Grid Margin="0,8" IsEnabled="{Binding HotkeysEnabled}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Fond d'écran précédent" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding HotkeyPrevious, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" TextAlignment="Center"/>
                            </Grid>
                            
                            <Grid Margin="0,8" IsEnabled="{Binding HotkeysEnabled}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Ajouter aux favoris" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding HotkeyFavorite, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" TextAlignment="Center"/>
                            </Grid>
                            
                            <Grid Margin="0,8" IsEnabled="{Binding HotkeysEnabled}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Pause/Reprise rotation" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding HotkeyPause, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" TextAlignment="Center"/>
                            </Grid>
                            
                            <Button Content="🔄 Appliquer les raccourcis" Command="{Binding ReloadHotkeysCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Margin="0,8"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Transitions -->
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="✨ Effets de transition" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                            <CheckBox Content="Activer les effets de transition" IsChecked="{Binding TransitionEnabled}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,8"/>
                            
                            <Grid Margin="0,8" IsEnabled="{Binding TransitionEnabled}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Type de transition" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                <ComboBox Grid.Column="1" ItemsSource="{Binding TransitionEffects}" SelectedItem="{Binding SelectedTransitionEffect}" Height="32"/>
                            </Grid>
                            
                            <Grid Margin="0,8" IsEnabled="{Binding TransitionEnabled}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Durée (ms)" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Text="{Binding TransitionDuration}" Style="{StaticResource ModernTextBox}" TextAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- APIs -->
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="🔑 Clés API (Sources en ligne)" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                            
                            <TextBlock Text="Unsplash (Access Key)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,8,0,4"/>
                            <TextBox Text="{Binding UnsplashApiKey, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                            
                            <TextBlock Text="Pexels (API Key)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,12,0,4"/>
                            <TextBox Text="{Binding PexelsApiKey, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                            
                            <TextBlock Text="Pixabay (API Key)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,12,0,4"/>
                            <TextBox Text="{Binding PixabayApiKey, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                            
                            <TextBlock Text="Obtenez vos clés gratuitement sur unsplash.com, pexels.com et pixabay.com" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Fonds d'écran animés" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                            <CheckBox Content="Mettre en pause sur batterie" IsChecked="{Binding PauseOnBattery}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,8"/>
                            <CheckBox Content="Mettre en pause en plein écran" IsChecked="{Binding PauseOnFullscreen}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,8"/>
                            <Button Content="Arrêter le fond animé" Command="{Binding StopAnimatedWallpaperCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Margin="0,8"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Maintenance" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                            <Button Content="🗑️ Vider le cache" Command="{Binding ClearThumbnailCacheAsyncCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left"/>
                            <Button Content="🔍 Détecter les doublons" Command="{Binding ScanDuplicatesCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Margin="0,8,0,0" IsEnabled="{Binding IsDuplicateScanRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            <ItemsControl ItemsSource="{Binding DuplicateGroups}" Margin="0,16,0,0" Visibility="{Binding DuplicateGroups.Count, Converter={StaticResource CountToVisibilityConverter}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                                            <StackPanel>
                                                <TextBlock FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8">
                                                    <Run Text="{Binding Wallpapers.Count, Mode=OneWay}"/> fichiers identiques
                                                </TextBlock>
                                                <ItemsControl ItemsSource="{Binding Wallpapers}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid Margin="0,2">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="50"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <Image Source="{Binding FilePath, Converter={StaticResource PathToThumb}}" Width="44" Height="30" Stretch="UniformToFill"/>
                                                                <TextBlock Grid.Column="1" Text="{Binding DisplayName}" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" Margin="8,0" TextTrimming="CharacterEllipsis"/>
                                                                <Button Grid.Column="2" Content="Retirer" Command="{Binding DataContext.RemoveDuplicateCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}" Background="#EF4444" Padding="8,4" FontSize="11"/>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                    
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="À propos" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                            <TextBlock Text="Wallpaper Manager v2.1.0" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="© 2025 Felix-Antoine" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
        
        <!-- Loading overlay -->
        <Border Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" Grid.ColumnSpan="2">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⏳" FontSize="48" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="White" FontSize="14" HorizontalAlignment="Center" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
