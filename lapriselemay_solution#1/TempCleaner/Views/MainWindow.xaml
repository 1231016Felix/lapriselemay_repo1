<Window x:Class="TempCleaner.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TempCleaner.ViewModels"
        xmlns:converters="clr-namespace:TempCleaner.Converters"
        Title="TempCleaner - Nettoyeur intelligent" 
        Height="700" Width="1100"
        MinHeight="550" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Icon="/Resources/app.ico"
        Background="{StaticResource BackgroundBrush}">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <converters:FileSizeConverter x:Key="FileSizeConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis"/>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🧹 TempCleaner" FontSize="28" FontWeight="SemiBold" 
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <!-- Badge Admin -->
                    <Border Background="#4CAF50" CornerRadius="4" Padding="8,4" Margin="12,0,0,0"
                            VerticalAlignment="Center"
                            Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="🔓 ADMIN" FontSize="11" FontWeight="Bold" Foreground="White"/>
                    </Border>
                </StackPanel>
                <TextBlock Text="Nettoyeur de fichiers temporaires intelligent" 
                           FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            
            <!-- Menu Outils système -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Menu Background="Transparent" VerticalAlignment="Center" Margin="0,0,12,0">
                    <MenuItem Header="🔧 Outils" FontWeight="SemiBold" Padding="12,8">
                        <!-- Purge Mémoire -->
                        <MenuItem Header="💾 Purge Mémoire">
                            <MenuItem Header="🚀 Purge MAXIMALE (tout)" Command="{Binding PurgeMaxMemoryCommand}"/>
                            <MenuItem Header="💾 Purge standard" Command="{Binding PurgeAllMemoryCommand}"/>
                            <Separator/>
                            <MenuItem Header="📊 Working Set uniquement" Command="{Binding PurgeWorkingSetCommand}"/>
                            <MenuItem Header="📋 Standby List (admin)" Command="{Binding PurgeStandbyListCommand}"/>
                            <MenuItem Header="📝 Modified Page List (admin)" Command="{Binding PurgeModifiedPageListCommand}"/>
                            <MenuItem Header="⬇️ Low-Priority Standby (admin)" Command="{Binding PurgeLowPriorityStandbyCommand}"/>
                        </MenuItem>
                        <Separator/>
                        <!-- Nettoyage système -->
                        <MenuItem Header="🧹 Nettoyage Système">
                            <MenuItem Header="🗑️ Vider la corbeille" Command="{Binding EmptyRecycleBinCommand}"/>
                            <MenuItem Header="🌐 Vider cache DNS" Command="{Binding FlushDnsCacheCommand}"/>
                            <MenuItem Header="📋 Vider presse-papiers" Command="{Binding ClearClipboardCommand}"/>
                            <MenuItem Header="📂 Effacer documents récents" Command="{Binding ClearRecentDocumentsCommand}"/>
                            <Separator/>
                            <MenuItem Header="🛒 Vider cache Windows Store" Command="{Binding ClearWindowsStoreCacheCommand}"/>
                            <MenuItem Header="🧽 Nettoyage de disque Windows" Command="{Binding RunDiskCleanupCommand}"/>
                        </MenuItem>
                        <Separator/>
                        <!-- Avancé -->
                        <MenuItem Header="⚡ Avancé (admin)">
                            <MenuItem Header="📦 Nettoyer WinSxS (composants obsolètes)" Command="{Binding CleanupWinSxSCommand}"/>
                            <MenuItem Header="😴 Activer/Désactiver hibernation" Command="{Binding ToggleHibernationCommand}"/>
                        </MenuItem>
                    </MenuItem>
                </Menu>
                
                <!-- Bouton Relancer en Admin -->
                <Button Command="{Binding RestartAsAdminCommand}"
                        Visibility="{Binding IsAdmin, Converter={StaticResource InverseBoolToVis}}"
                        Background="#FF5722" Foreground="White"
                        Padding="16,10" FontWeight="SemiBold"
                        Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔒" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <StackPanel>
                                    <TextBlock Text="Relancer en Admin" FontWeight="SemiBold"/>
                                    <TextBlock FontSize="10" Opacity="0.8">
                                        <Run Text="{Binding AdminProfileCount, Mode=OneWay}"/> catégories supplémentaires
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            </StackPanel>
        </Grid>

        <!-- Profils de nettoyage -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16" MaxHeight="220">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="📋 Catégories à analyser" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Profiles}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,8,8" Padding="10,6" CornerRadius="6"
                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                        Background="{StaticResource SurfaceBrush}">
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay}" 
                                                  VerticalAlignment="Center"
                                                  Checked="ProfileCheckBox_Checked"
                                                  Tag="{Binding}"/>
                                        <TextBlock Text="{Binding Icon}" FontSize="14" Margin="6,0,4,0" VerticalAlignment="Center"/>
                                        <StackPanel VerticalAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Name}" FontWeight="Medium" FontSize="12"/>
                                                <TextBlock Text="⚠️" FontSize="9" Margin="4,0,0,0" VerticalAlignment="Center"
                                                           Visibility="{Binding IsSafe, Converter={StaticResource InverseBoolToVis}}"
                                                           ToolTip="Opération risquée"/>
                                                <TextBlock Text="🔒" FontSize="9" Margin="4,0,0,0" VerticalAlignment="Center"
                                                           Visibility="{Binding RequiresAdmin, Converter={StaticResource BoolToVis}}"
                                                           ToolTip="Nécessite les droits administrateur"/>
                                                <TextBlock Text="👁️" FontSize="9" Margin="4,0,0,0" VerticalAlignment="Center"
                                                           Visibility="{Binding IsPrivacy, Converter={StaticResource BoolToVis}}"
                                                           ToolTip="Données de confidentialité"/>
                                            </StackPanel>
                                            <TextBlock FontSize="10" Foreground="{StaticResource TextSecondaryBrush}">
                                                <Run Text="{Binding FileCount, Mode=OneWay}"/> fichiers - 
                                                <Run Text="{Binding TotalSizeFormatted, Mode=OneWay}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Liste des fichiers -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <DataGrid 
                          x:Name="FilesDataGrid"
                          ItemsSource="{Binding FilteredFiles}"
                          AutoGenerateColumns="False"
                          IsReadOnly="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          Background="Transparent"
                          RowBackground="{StaticResource SurfaceBrush}"
                          AlternatingRowBackground="#FAFAFA"
                          SelectionMode="Extended"
                          SelectionUnit="FullRow">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="✓" Width="50">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Checked="CheckBox_Changed" Unchecked="CheckBox_Changed"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Nom" Binding="{Binding FileName}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Taille" Binding="{Binding SizeFormatted}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Catégorie" Binding="{Binding Category}" Width="180" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Modifié" Binding="{Binding LastModified, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" Width="140" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Chemin" Binding="{Binding RelativePath}" Width="250" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Message si aucun fichier -->
                <Border 
                        Visibility="{Binding FilteredFiles.Count, Converter={StaticResource InverseBoolToVis}}"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="📂" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        <TextBlock Text="Cliquez sur 'Analyser' pour rechercher les fichiers temporaires"
                                   HorizontalAlignment="Center"
                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="14"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Barre de progression -->
        <Grid Grid.Row="3" Margin="0,16,0,0"
              Visibility="{Binding IsWorking, Converter={StaticResource BoolToVis}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ProgressBar Grid.Column="0" Value="{Binding ProgressPercent}" 
                         Style="{StaticResource ModernProgressBar}" Height="8"/>
            <TextBlock Grid.Column="1" Text="{Binding StatusMessage}" 
                       Margin="12,0,0,0" VerticalAlignment="Center"
                       Foreground="{StaticResource TextSecondaryBrush}"/>
        </Grid>

        <!-- Footer avec stats et boutons -->
        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Stats -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="#FFF3E0" Padding="12,8" CornerRadius="6" Margin="0,0,12,0"
                        ToolTip="Clic droit pour vider">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🗑️ Corbeille:" FontWeight="Medium" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding RecycleBinCount}" FontWeight="Bold" Foreground="#E65100"/>
                        <TextBlock Text=" (" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Text="{Binding RecycleBinSizeFormatted}" FontWeight="Medium" Foreground="#E65100"/>
                        <TextBlock Text=")" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Background="#E3F2FD" Padding="12,8" CornerRadius="6" Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊 Total:" FontWeight="Medium" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding TotalCount}" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text=" fichiers (" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Text="{Binding TotalSizeFormatted}" FontWeight="Medium" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text=")" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Background="#E8F5E9" Padding="12,8" CornerRadius="6">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✓ Sélection:" FontWeight="Medium" Margin="0,0,8,0" Foreground="#2E7D32"/>
                        <TextBlock Text="{Binding SelectedCount}" FontWeight="Bold" Foreground="#2E7D32"/>
                        <TextBlock Text=" fichiers (" Foreground="#5C5C5C"/>
                        <TextBlock Text="{Binding SelectedSizeFormatted}" FontWeight="Medium" Foreground="#2E7D32"/>
                        <TextBlock Text=")" Foreground="#5C5C5C"/>
                    </StackPanel>
                </Border>
                <TextBlock Text="{Binding ScanDuration}" Margin="16,0,0,0" 
                           VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>

            <!-- Boutons d'action -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="🔍 Analyser" Style="{StaticResource ModernButton}"
                        Command="{Binding ScanCommand}" Width="140" Margin="0,0,12,0"/>
                <Button Content="🗑️ Nettoyer" Style="{StaticResource DangerButton}"
                        Command="{Binding CleanCommand}" Width="140" Margin="0,0,12,0"/>
                <Button Content="❌ Annuler" Style="{StaticResource SecondaryButton}"
                        Command="{Binding CancelCommand}" Width="100"
                        Visibility="{Binding CanCancel, Converter={StaticResource BoolToVis}}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
